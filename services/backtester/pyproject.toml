[project]
name = "tradelens-backtester"
version = "0.1.0"
description = "TradeLens event-driven backtesting service"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
